# First Init

When you don't init ur info in git bash (which is now I'm using), you will always have to write down your Github Id and Password. 

How to : 

+ git config user.email "이메일"
+ git config user.name "Tom"

By writing down two commands above, we will be able to use Github conveniently.

